CREATE TABLE BOOK
(
    ID            BIGINT NOT NULL,
    DESCRIPTION   VARCHAR(255),
    ILLUSTRATIONS BOOLEAN,
    ISBN          VARCHAR(255),
    NBOFPAGES     INTEGER,
    PRICE         FLOAT,
    TITLE         VARCHAR(255),
    PRIMARY KEY (ID)
)
